name: A worlkflow fro my Hello World file
on: push

jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-lastest
    steps: 
      - uses: actions/checkout@v1
      - uses: ./action-a
        with:
          MY_NAME: "Mona"
        
        
